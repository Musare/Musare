services:
  backend:
    volumes:
      - ./.git:/opt/.git:ro
      - ./common:/opt/common
      - ./types:/opt/types
      - ./backend:/opt/app

  frontend:
    volumes:
      - ./.git:/opt/.git:ro
      - ./common:/opt/common
      - ./types:/opt/types
      - ./frontend:/opt/app
